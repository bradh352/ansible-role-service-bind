---
- name: reload_bind
  ansible.builtin.service:
    name: "{{ 'bind' if ansible_os_family == 'Debian' else 'named' }}"
    state: reloaded

- name: restart_bind
  ansible.builtin.service:
    name: "{{ 'bind' if ansible_os_family == 'Debian' else 'named' }}"
    state: restarted
